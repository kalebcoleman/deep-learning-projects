# Deep Learning Projects (Spring 2026)

This repo contains my Deep Learning course projects.

## Structure
- `project-01/` … `project-04/`: Each project is self-contained (code, notebooks, writeups).
- `shared/`: Reusable helpers/utilities across projects.

## How to run
Each project has its own setup instructions inside its folder.

## Notes
Large files (datasets, model checkpoints, results) are intentionally ignored via `.gitignore`.

